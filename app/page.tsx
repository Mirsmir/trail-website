import TrailExperience from '@/components/trail/TrailExperience';

/** The home page is the ride. Everything about it lives in components/trail. */
export default function Home() {
  return <TrailExperience />;
}
